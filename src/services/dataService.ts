// Placeholder for data service functions
export const fetchData = async () => {
  // Simulate fetching data
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve({ message: 'Data fetched successfully!' });
    }, 1000);
  });
};
